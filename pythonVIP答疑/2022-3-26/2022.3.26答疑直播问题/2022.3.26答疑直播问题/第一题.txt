我这第一步就卡住了，3/4行仅仅是想要获取文件路径
第5行起的循环在6行就出了问题，报错为：
OSError: [Errno 22] Invalid argument
<_io.BufferedReader name='.\\new.pdf'>

而我想要的是./new.pdf这样的路径，等等。

1：目标找到指定文件路径及子目录下所有文件
2：删除第二页和最后一页的pdf页面
3：根据源文件路径保存文件（新文件存储为filename-new.pdf）就是增加-new来保存

本应该是很简单的东西，结果我卡住两个点：
1:3/4行已经搞出的路径在下面没法引用
2：现在能知道当前文件是多少页，但不知道如何删除最后一页，暂时只能写成绝对值。想要相对值（因为路径下的文件有N多个不同页数的pdf，少则5-10页，多则几百页）

如果能做页面内容的判定，也就是没文字没图片，也一并删除就更完美。但我还没弄明白怎么玩


代码如下：

import PyPDF2,glob
filenames=[]
for filename in glob.glob(r'./*.pdf'):
    filenames.append(filename)
for i in filenames:
    pdffile = open(i,'rb')
    print(pdffile)
    pdfreader = PyPDF2.PdfFileReader(pdffile)
    print(pdfreader)
    # pdfwrite1 = PyPDF2.PdfFileWriter()
    # all_page = pdfreader.numPages
    # for page in range(all_page):
    #     #去除不想要的页面
    #     if page in [1,2,all_page-1,all_page-2,all_page-3]:
    #         continue
    #     pageobj = pdfreader.getPage(page)
    #     # print(pageobj)
    #     pdfwrite1.addPage(pageobj)
    # f1 = open(i,'wb')
    # pdfwrite1.write(f1)
    # pdffile.close()
    # f1.close()

