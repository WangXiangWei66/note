动态代理的原理：
(1)动态IP到哪里去弄？
    芝麻代理， 比特代理 http://www.bitdaili.com/
    马士兵教育的三天体验课（1秒钟切换一个IP） ， 比特代理

（2）IP地址放入到List中   （IP池）

（3）每次发送请求时，可以使用random.choice()函数，从列表中任选一个IP，构建请求

列表中的元素可以是任意数据类型
Python中的组合数据结构有哪些？列表，元组，集合，字典  ,四者中最常用提列表和字典

元组是不可变对象
列表、集合、字典 可变对象


列表的底层数据结构？
列表的底层数据结构是顺序表
Python中的列表采用的是分离式顺序表  （表头和数据分开，表头通过引用指向数据）


percentile（）函数的计算原理，怎么得出的返回值
 numpy中的函数
 python中没有数组 ，与数组对应的是列表
 numpy中有数扰array


yield与return的区别
共同点：应用在函数中


空心三解形问题





